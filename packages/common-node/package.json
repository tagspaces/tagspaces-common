{
  "name": "@tagspaces/tagspaces-common-node",
  "description": "TagSpaces common node module",
  "version": "3.0.4",
  "author": "TagSpaces Authors <contact@tagspaces.org>",
  "license": "MIT",
  "scripts": {
    "clean-install": "npx rimraf package-lock.json && npx rimraf node_modules && npm install",
    "npmpack": "npm run clean-install && npm pack && tar -xvf tagspaces-tagspaces-common-node-2.1.25.tgz && npx shx cp -rL package/** ../../../tagspaces/node_modules/@tagspaces/tagspaces-common-node && rimraf package",
    "pkg-publish": "npm run clean-install && npm publish --access public"
  },
  "dependencies": {
    "@tagspaces/tagspaces-common": "^3.0.4",
    "fs-extra": "^10.0.0",
    "jszip": "^3.10.1",
    "jszip-utils": "^0.1.0",
    "klaw": "^4.1.0",
    "marked": "^3.0.8"
  }
}
